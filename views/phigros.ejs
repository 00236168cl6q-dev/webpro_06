<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>phigros 最高難易度曲(Level:AT)一覧</title>
  <style>
    body {
    margin: 0;
    font-family: sans-serif;
    background-image: url("/public/background_111.png");
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    background-attachment: fixed;
    color: #fff; /* 全体文字を白系に */
    }
    h2 {
    margin: 20px;
    color: #fff;
    text-shadow: 0 2px 6px rgba(0,0,0,0.7);
    }
    table {
    border-collapse: collapse;
    background: rgba(10, 15, 30, 0.95); /* 濃紺ベース */
    color: #e6f0ff;
    box-shadow:
    0 0 20px rgba(0, 200, 255, 0.35),
    0 0 40px rgba(160, 80, 255, 0.2);
    }
    th, td {
    border: 1px solid rgba(0, 200, 255, 0.4);
    padding: 10px 14px;
    }
    th {
    background: linear-gradient(
    135deg,
    rgba(30, 60, 120, 0.95),
    rgba(80, 40, 140, 0.95)
    );
    color: #ffffff;
    font-weight: bold;
    text-shadow: 0 0 6px rgba(180, 120, 255, 0.8);
    }
    td {
    text-shadow: 0 0 4px rgba(0, 180, 255, 0.6);
    }
    a {
    color: #7fe9ff;
    font-weight: bold;
    text-decoration: none;
    }
    a:hover {
    color: #c89bff;
    text-shadow: 0 0 8px rgba(200, 150, 255, 0.9);
    }
    body > a {
    display: inline-block;
    margin: 15px 20px;
    color: #4da3ff;
    font-weight: bold;
    }
  </style>
</head>
<body>
  <h2>Phigros 最高難易度曲(Level:AT)一覧</h2>
  <table>
    <tr><th>曲名</th><th>難易度</th></tr>
      <% let number = 0 %>
      <% for( let row of data ) { %>
        <tr><td><a href="/phigros/<%= number %>"><%= row.name %></a></td>
        <td><%= row.level %></td>
        <% number += 1 %>
      <% } %>
  </table>
  <a href="/phigros/create">曲の情報を追加</a>
</body>
</html>