<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>「<%= data.name %>」の詳細</title>
  <style>
    body {
      margin: 0;
      padding: 30px;
      font-family: sans-serif;
      background-color: #111;
      color: #e0e0e0;
    }
    h2 {
      margin-bottom: 20px;
      color: #f2f2f2;
      font-weight: 600;
    }
    img {
      display: block;
      margin-bottom: 25px;
      max-width: 100%;
      border: 1px solid #444;
    }
    table {
      border-collapse: collapse;
      width: 100%;
      max-width: 1000px;
      background-color: #1a1a1a;
      color: #ddd;
      margin-bottom: 25px;
    }
    th, td {
      border: 1px solid #444;
      padding: 10px 14px;
    }
    th {
      background-color: #222;
      color: #f0f0f0;
      font-weight: bold;
      width: 30%;
    }
    td {
      color: #ddd;
    }
    a {
      margin-right: 15px;
      color: #9fc5ff;
      text-decoration: none;
    }
    a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <h2>「<%= data.name %>」の詳細</h2>
  <img src="<%= icon.filename %>" alt="<%= icon.alt %>" width="1000">
  <table>
    <tr><th>項目</th><th>データ</th></tr>
    <tr><td>曲名</td><td><%= data.name %></td></tr>
    <tr><td>曲作者名</td><td><%= data.author_music %></td></tr>
    <tr><td>譜面作者名</td><td><%= data.author_notes %></td></tr>
    <tr><td>難易度</td><td><%= data.level %></td></tr>
    <tr><td>速度</td><td><%= data.bpm %>bpm</td></tr>
    <tr><td>ノーツ数</td><td><%= data.notes_figure %>ノーツ</td></tr>
    <tr><td>ノーツ密度</td><td><%= data.notes_density %>ノーツ/秒</td></tr>
    <tr><td>曲の時間</td><td><%= data.time %>秒</td></tr>
  </table>
  <a href="/phigros/edit/<%= id %>">編集</a>
  <a href="/phigros/delete/<%= id %>">削除</a>
  <a href="/phigros">曲一覧に戻る</a>
</body>
</html>