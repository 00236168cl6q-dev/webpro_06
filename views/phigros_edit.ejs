<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>「<%= data.name %>」の編集</title>
  <style>
    body {
      margin: 0;
      padding: 30px;
      font-family: sans-serif;
      background-color: #111;
      color: #e0e0e0;
    }
    h2 {
      margin-bottom: 20px;
      color: #f2f2f2;
      font-weight: 600;
    }
    table {
      border-collapse: collapse;
      width: 100%;
      max-width: 1000px;
      background-color: #1a1a1a;
      color: #ddd;
      margin-bottom: 20px;
    }
    th, td {
      border: 1px solid #444;
      padding: 10px 14px;
    }
    th {
      background-color: #222;
      color: #f0f0f0;
      font-weight: bold;
      width: 30%;
    }
    input[type="text"],
    input[type="number"] {
      width: 98%;
      padding: 6px 8px;
      font-size: 14px;
      background-color: #222;
      border: 1px solid #555;
      color: #eee;
    }
    input[type="text"]:focus,
    input[type="number"]:focus {
      outline: none;
      border-color: #888;
      background-color: #262626;
    }
    input[type="submit"] {
      padding: 8px 22px;
      font-size: 14px;
      background-color: #333;
      border: 1px solid #666;
      color: #eee;
      cursor: pointer;
    }
    input[type="submit"]:hover {
      background-color: #444;
    }
    a {
      margin-right: 15px;
      color: #9fc5ff;
      text-decoration: none;
    }
    a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <h2>「<%= data.name %>」の編集</h2>
  <form action="/phigros/update/<%= id %>" method="POST">
    <table>
      <tr><th>項目</th><th>データ</th></tr>
      <tr><td>曲名</td><td><input type="text" name="name" value="<%= data.name %>"></td></tr>
      <tr><td>曲作者名</td><td><input type="text" name="author_music" value="<%= data.author_music %>"></td></tr>
      <tr><td>譜面作者名</td><td><input type="text" name="author_notes" value="<%= data.author_notes %>"></td></tr>
      <tr><td>難易度</td><td><input type="number" step="0.1" name="level" value="<%= data.level %>"></td></tr>
      <tr><td>速度</td><td><input type="text" name="bpm" value="<%= data.bpm %>"></td></tr>
      <tr><td>ノーツ数</td><td><input type="number" name="notes_figure" value="<%= data.notes_figure %>"></td></tr>
      <tr><td>ノーツ密度</td><td><input type="number" step="0.01" name="notes_density" value="<%= data.notes_density %>"></td></tr>
      <tr><td>曲の時間</td><td><input type="number" name="time" value="<%= data.time %>"></td></tr>
      <tr><td>画像ファイル名</td><td><input type="text" name="image" value="<%= data.image %>"></td></tr>
    </table>
    <input type="submit" value="登録">
  </form>
  <a href="/phigros/">曲一覧に戻る</a>
  <a href="/phigros/<%= id %>">詳細に戻る</a>
</body>
</html>