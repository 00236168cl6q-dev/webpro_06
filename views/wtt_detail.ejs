<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>「<%= data.name %>」の詳細</title>
  <style>
  body {
      margin: 0;
      padding: 30px;
      background-color: #000;
      color: #eee;
      font-family: sans-serif;
    }
    h2 {
      margin-bottom: 20px;
      color: #fff;
    }
    img {
      display: block;
      margin-bottom: 20px;
      max-width: 100%;
      border: 3px solid #444;
    }
    table {
      border-collapse: collapse;
      width: 100%;
      max-width: 800px;
      background-color: #b8b6b6;
      color: #222;
      margin-bottom: 20px;
      box-shadow: 0 8px 20px rgba(0,0,0,0.6);
    }
    th, td {
      border: 1px solid #aaa;
      padding: 10px 14px;
    }
    th {
      background-color: #ff5900;
      font-weight: bold;
      width: 30%;
    }
    a {
      color: #4da3ff;
      margin-right: 15px;
      text-decoration: none;
      font-weight: bold;
    }
    a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <h2>「<%= data.name %>」の詳細</h2>
  <img src="<%= icon.filename %>" alt="<%= icon.alt %>" width="800">
  <table>
    <tr><th>項目</th><th>データ</th></tr>
    <tr><td>選手名</td><td><%= data.name %></td></tr>
    <tr><td>代表国</td><td><%= data.country %></td></tr>
    <tr><td>年齢</td><td><%= data.age %>歳</td></tr>
    <tr><td>世界ランキング</td><td><%= data.world_ranking %>位</td></tr>
    <tr><td>過去の成績</td><td><%= data.result %></td></tr>
    <tr><td>利き手</td><td><%= data.hand %></td></tr>
    <tr><td>戦型（ラケット）</td><td><%= data.type %></td></tr>
  </table>
  <a href="/wtt/edit/<%= id %>">編集</a>
  <a href="/wtt/delete/<%= id %>">削除</a>
  <a href="/wtt">選手一覧に戻る</a>
</body>
</html>